upstream django {
    server web:8000;
}

server {
    listen 80;
    server_name postgres-u67346.vm.elestio.app;  # Remplacer par votre domaine

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirection HTTPS (désactivé initialement)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # Configuration temporaire HTTP (à supprimer après SSL)
    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
        add_header Cache-Control "public";
    }
}

# Configuration HTTPS (à décommenter après obtention du certificat SSL)
# server {
#     listen 443 ssl http2;
#     server_name postgres-u67346.vm.elestio.app;
#
#     ssl_certificate /etc/letsencrypt/live/postgres-u67346.vm.elestio.app/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/postgres-u67346.vm.elestio.app/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_redirect off;
#     }
#
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /media/ {
#         alias /app/media/;
#         expires 7d;
#         add_header Cache-Control "public";
#     }
# }
